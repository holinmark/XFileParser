#pragma once

/*

msbuild /m:4 /p:Configuration=Debug XFileParser.sln
msbuild /m:4 /p:Configuration=Release XFileParser.sln

*/

#include "resource.h"

int APIENTRY wWinMain(HINSTANCE, HINSTANCE, LPWSTR, int);
